version: 2

project_name: eru-cli

before:
  hooks:
    - go mod download

builds:
  - id: eru-cli-debug
    binary: eru-cli.dbg
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/projecteru2/cli/version.REVISION={{.Commit}}
      - -X github.com/projecteru2/cli/version.VERSION={{.Env.VERSION}}
      - -X github.com/projecteru2/cli/version.BUILTAT={{.Date}}
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    main: .

  - id: eru-cli-release
    binary: eru-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s
      - -X github.com/projecteru2/cli/version.REVISION={{.Commit}}
      - -X github.com/projecteru2/cli/version.VERSION={{.Env.VERSION}}
      - -X github.com/projecteru2/cli/version.BUILTAT={{.Date}}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    main: .

archives:
  - id: release
    ids:
      - eru-cli-release
    name_template: >-
      {{- .ProjectName }}_{{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

  - id: debug
    ids:
      - eru-cli-debug
    name_template: >-
      {{- .ProjectName }}_{{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}_debug
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

  # Unversioned alias so releases/latest/download/eru-cli_Darwin_arm64.tar.gz resolves.
  - id: release-latest
    ids:
      - eru-cli-release
    name_template: >-
      {{- .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

checksum:
  name_template: 'checksums.txt'

release:
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
